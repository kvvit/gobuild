#!groovy
properties([disableConcurrentBuilds()])

pipeline {
  agent {
        label 'gobuild'
        }
  options {
        buildDiscarder(logRotator(numToKeepStr: '5', artifactNumToKeepStr: '10'))
        timestamps()
    }
  stages {
    stage("Build") {
      steps {
        sh 'export PATH=$PATH:/usr/local/go/bin && go version'
        sh 'export PATH=$PATH:/usr/local/go/bin && go build main.go'
      }
    }
  }
}
